# 技术债清单

记录已知但暂未处理的问题，避免被 ESLint 降级规则掩盖。**新代码不得新增此处列出的问题类型。**

CI 中 `npm run lint -- --max-warnings 360` 设有上限，这个数字只允许往下调。

## 一、React 19 编译期规则命中（11 处，运行时正常但写法需重构）

这些是 `eslint-config-next` 随 React 19 引入的新规则，命中的都是存量写法。

### `react-hooks/set-state-in-effect` — effect 内同步 setState 导致级联渲染

| 文件 | 位置 |
|---|---|
| `src/app/admin/contests/[id]/page.tsx` | 74:7 |
| `src/app/admin/exams/page.tsx` | 74:21 |
| `src/app/exams/attempt/[id]/page.tsx` | 53:21、117:27 |
| `src/app/problems/[id]/page.tsx` | 44:5 |

处理方向：能在渲染期直接派生的状态不要放进 effect；确需异步的改为在事件回调或 `useSyncExternalStore` 中更新。
考试作答页（`exams/attempt/[id]`）是倒计时 + 防切屏的核心，改动需连带回归倒计时自动交卷与切屏计数。

### `react-hooks/immutability` — 在声明前访问变量（4 处）

`src/app/admin/login/page.tsx:22`、`src/app/knowledge/page.tsx:21`、`src/app/login/page.tsx:23`、`src/app/problems/[id]/page.tsx:29`

均为 `useEffect` 依赖数组引用了下方 `const` 声明的函数。处理方向：把函数用 `useCallback` 提到 effect 之前，或直接把逻辑内联进 effect。

### `react-hooks/refs` — 渲染期访问 ref（1 处）

`src/app/exams/attempt/[id]/page.tsx:219`

处理方向：渲染期需要的值放 state，ref 只在事件回调与 effect 中读写。

## 二、类型债：约 320 处 `any`

集中在 `src/lib/`（`db.ts`、`judgeDaemon.ts`、`judgeExecutor.ts`、`examGrade.ts`、`localKnowledgeJudge.ts`）与各 API 路由的数据库查询返回值上。

处理方向：为数据库行定义 `interface`（如 `ProblemRow`、`SolutionRow`、`ExamPaperRow`）放在 `src/types/` 下，`query<T>()` 泛型化后逐模块替换。建议按模块拆成多个小 PR，一次收一个模块。

## 三、其他

- `src/components/BootstrapClient.tsx:7` 使用 `require()` 动态加载 Bootstrap JS，待改为 `await import()`
- 4 处 JSX 文案中的裸引号（`react/no-unescaped-entities`），纯显示问题，顺手改即可

## 四、测试覆盖

目前没有自动化测试，判题与考试判分依赖手工验证。
建议优先补：`judgeExecutor` 各语言编译/运行的单元测试、`examGrade` 五种题型判分的单元测试。这两处逻辑错误的代价最高（判错分、误判 AC）。
