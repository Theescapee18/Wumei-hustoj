# 安全策略

## 报告漏洞

如果你发现涉及**权限绕过、SQL 注入、判题沙箱逃逸、考试答案泄露、越权读取他人提交/成绩**等安全问题，**请不要提交公开 Issue**。

请通过 GitHub [Security Advisories](https://github.com/Theescapee18/Wumei-hustoj/security/advisories/new) 私下报告，包含：

- 受影响的接口或页面路径
- 复现步骤（可附最小化的请求示例，请去除真实凭据）
- 你评估的影响范围

我们会尽快确认并修复，修复发布后再公开披露细节。

## 部署方须自行落实的事项

本项目默认配置面向社团内网/小规模使用，公网部署前请务必：

1. **修改默认管理员密码**：初始化脚本内置 `admin / admin`，首次登录后立即修改
2. **替换 `JWT_SECRET`**：使用随机长字符串，`openssl rand -base64 32`
3. **修改数据库密码**：不要沿用 `.env.example` 中的占位值
4. **不要把 `.env` / `.env.local` 提交到仓库**（已在 `.gitignore` 中排除）
5. **判题隔离**：判题通过子进程执行用户提交的代码，容器内已限制内存与超时，但**不要以 root 在宿主机裸跑**，务必使用提供的 Docker 部署方式
6. **反向代理与 HTTPS**：公网暴露时在前面加 Nginx/Caddy 并启用 HTTPS，Cookie 才能以 Secure 属性生效

## 已知边界

- 判题依赖进程级超时与内存限制，不是完整的安全沙箱（无 seccomp/cgroup 级隔离）。仅适用于**可信用户群体**（社团成员），不要开放匿名提交。
- 用户账号由管理员导入，系统不提供自助注册，这是有意的设计。
